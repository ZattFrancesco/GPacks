Ici tu ranges tes gros systèmes (tickets, economy, logs, levels, etc.)
Chaque module peut avoir :
- un dossier `db/` (si tu utilises une DB)
- un `index.js` ou `service.js`
- des utils propres au module
